import React from 'react';
import { View, Text } from 'react-native';

const SettingsScreen = () => (
  <View>
    <Text>Settings: Update location for weather (integrate form to backend)</Text>
  </View>
);

export default SettingsScreen;